import '.././globals.css'
import Link from "next/link"
import { Inter } from 'next/font/google'

// Icons 
import { GoFileDirectoryFill } from "react-icons/go"

const inter = Inter({ subsets: ['latin'] })

export const metadata = {
    title: 'Create Next App',
    description: 'Generated by create next app',
}

export default function LibraryLayout({ children }) {
    const mygames = [
        "The Hustle",
        "Trapped Angler 1",
        "Trapped Angler 2",
    ];
    return (
        <div className="flex h-[100%]">
            <div className=" w-[30%] min-w-[250px] max-w-[300px] flex flex-col overflow-auto">
                <p className="text-lg mb-2"><b>My Games</b></p>
                {mygames.map((mygame) => (
                    <Link href='/library/game' className="group flex p-3 gap-2 border-r-4 border-transparent text-grey hover:border-accent hover:bg-gradient-to-l from-accent-50 to-transparent hover:text-accent">
                        <GoFileDirectoryFill className=" text-lg " />
                        <p className="group-hover:text-white">{mygame}</p>
                    </Link>
                ))}


            </div>
            <div className="w-[100%] h-[100%] overflow-auto border-accent-50 border-l-[1px]">
                {children}
            </div>
        </div>
    )
}

